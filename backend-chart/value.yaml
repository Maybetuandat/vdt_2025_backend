replicaCount: 1

image:
  repository:
  tag: "2.0"
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 8000
  nodePort: 30002

database:
  host: vdt-db-postgres-chart
  port: 5432
  name: vdt_db
  user: postgres
  password: "123456"

resources: {}
